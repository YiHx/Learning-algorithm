#include <bits/stdc++.h>
using namespace std;
vector<vector<int>> b;
vector<vector<int>> a;

void solve () {
    int n,m;
    cin>>m>>n;
    for (int i=0;i<m;i++) {
        int curr;
        cin>>curr;
        for (int j=0;j<curr;j++) {

        }
    }
}
int main () {
    ios::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}
