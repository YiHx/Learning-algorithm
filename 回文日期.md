### 题目描述
在日常生活中，通过年、月、日这三个要素可以表示出一个唯一确定的日期。

牛牛习惯用 $8$ 位数字表示一个日期，其中，前 $4$ 位代表年份，接下来 $2$ 位代表月份，最后 $2$ 位代表日期。

显然：一个日期只有一种表示方法，而两个不同的日期的表示方法不会相同。

牛牛认为，一个日期是回文的，当且仅当表示这个日期的 $8$ 位数字是回文的。

现在，牛牛想知道：在他指定的两个日期之间（包含这两个日期本身），有多少个真实存在的日期是回文的。

一个 $8$ 位数字是回文的，当且仅当对于所有的 $i$($1≤i≤8$) 从左向右数的第 $i$ 个数字和第 $9−i$ 个数字（即从右向左数的第 $i$ 个数字）是相同的。

例如：

*   对于 $2016$ 年 $11$ 月 $19$ 日，用 $8$ 位数字 $20161119$ 表示，它不是回文的。
*   对于 $2010$ 年 $1$ 月 $2$ 日，用 $8$ 位数字 $20100102$ 表示，它是回文的。
*   对于 $2010$ 年 $10$ 月 $2$ 日，用 $8$ 位数字 $20101002$ 表示，它不是回文的。

#### 输入格式

输入包括两行，每行包括一个 $8$ 位数字。

第一行表示牛牛指定的起始日期 $date_1$，第二行表示牛牛指定的终止日期 $date_2$。保证 $date_1$ 和 $date_2$ 都是真实存在的日期，且年份部分一定为 $4$ 位数字，且首位数字不为 $0$。

保证 $date_1$ 一定不晚于 $date_2$。

#### 输出格式

输出共一行，包含一个整数，表示在 $date_1$ 和 $date_2$ 之间，有多少个日期是回文的。

#### 输入样例：

```
20110101
20111231
```

#### 输出样例：

```
1
```

---
### 算法
枚举 模拟

#### C++ 代码
```
#include <bits/stdc++.h>
using namespace std;
 int beginn,endd;
int x;
int q;
int mid;
int res;
int temp;
int amount_day_1[13]={0,31,28,31,30,31,30,31,31,30,31,30,31};
int amount_day_2[13]={0,31,29,31,30,31,30,31,31,30,31,30,31};
int check(int year,int mouth,int day){
    if(mouth<1||mouth>12){
        return false;
    }
    if(((year%100==0)&&(year%400==0))||((year%100!=0)&&(year%4==0))){
    if(day >= 1 && day <= amount_day_2[mouth])return true;
    else return false;
    }
    else if(day >= 1 && day <= amount_day_1[mouth])return true;
    else return false;
}
int main () {
    int year,mouth,day;
     cin>>beginn>>endd;
    for(int i=1000;i<=9999;i++){
        year=i;
        temp=year;
        for(int j=1;j<=4;j++){
        mid=mid*10+temp%10;
        temp/=10;
        }
        mouth=mid/100;
        day=mid%100;
        mid=0;
        if(check(year,mouth,day)&&year*10000+mouth*100+day>=beginn&&year*10000+mouth*100+day<=endd){
            res++;
        }
    }
        cout<<res<<endl;
    return 0;
}
```
#### 反思
日期问题很重要的一个问题就是需要判断一下二月的天数，这就要判断一下是平年还是闰年，判断平年还是闰年有两种可能，第一是可以被100整除，那如果可以400整除，这个年份就是闰年，如果不能被100整除，但是可以被4整除，那么这个年份也是闰年，还有就是要注意一下拆分数字和组合数字的方式，查分数字就使用c++的特性，通过除和取模来获取每一个数字，还有就是把很多个数字整合成一个完整的数字的方法。