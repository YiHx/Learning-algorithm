### 题目描述
四平方和定理，又称为拉格朗日定理：

每个正整数都可以表示为至多 $4$ 个正整数的平方和。

如果把 $0$ 包括进去，就正好可以表示为 $4$ 个数的平方和。

比如：

$5 = 0^2 + 0^2 + 1^2 + 2^2$  
$7 = 1^2 + 1^2 + 1^2 + 2^2$

对于一个给定的正整数，可能存在多种平方和的表示法。

要求你对 $4$ 个数排序：

$0 \le a \le b \le c \le d$

并对所有的可能表示法按 $a,b,c,d$ 为联合主键升序排列，最后输出第一个表示法。

#### 输入格式

输入一个正整数 $N$。

#### 输出格式

输出4个非负整数，按从小到大排序，中间用空格分开。

#### 数据范围

$0 < N < 5 \* 10^6$

#### 输入样例：

```
5
```

#### 输出样例：

```
0 0 1 2
```

---
### 算法

二分


#### C++ 代码
```
#include <bits/stdc++.h>
using namespace std;
const int N = 5e6;
struct Sum{
    int s,c,d;
    bool operator<(const Sum &t)const{
        if(s!=t.s)return s<t.s;
        if(c!=t.c)return c<t.c;
        return d<t.d;
    }
}sum[N];
int main () {
    int n;
    int k=0;
    cin >> n;
    for(int c=0;c*c<=n;c++){
        for(int d=c;c*c+d*d<=n;d++){
          sum[k++]={c*c+d*d,c,d};
        }
    }
    sort(sum,sum+k);
    for(int a=0;a*a<=n;a++){
        for(int b =a;a*a+b*b<=n;b++){
            int m =n-a*a-b*b;
            int l=0;
            int r =k-1;
            while(l<r){
                int mid=(l+r)>>1;
                if(sum[mid].s>=m)r=mid;
                else l =mid +1;
            }
            if(sum[l].s==m){
            cout<<a<<" "<<b<<" "<<sum[l].c<<" "<<sum[l].d;return 0;}
        }
    }
    
    return 0;
}
```
#### 反思
重载运算符的方法还不是很会，以及不能很快的想出降低时间复杂度的方法（还是刷题刷太少了）